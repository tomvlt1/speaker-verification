# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored.

# Azure Speech resource key (Portal → your Speech resource → Keys and Endpoint)
AZURE_SPEECH_KEY=your_key_here

# Region of the Speech resource (e.g. westeurope, eastus, northeurope)
AZURE_SPEECH_REGION=westeurope
